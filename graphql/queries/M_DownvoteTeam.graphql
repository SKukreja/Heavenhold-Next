mutation DownvoteTeam($teamId: Int!, $userId: Int!, $ipAddress: String!) {
      downvoteTeam(input: { teamId: $teamId, userId: $userId, ipAddress: $ipAddress }) {
        teamId
        success
        currentVote
      }
    }