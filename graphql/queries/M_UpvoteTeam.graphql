mutation UpvoteTeam($heroId: Int!, $teamId: Int!, $userId: Int!, $ipAddress: String!) {
      upvoteTeam(input: { heroId: $heroId, teamId: $teamId, userId: $userId, ipAddress: $ipAddress }) {
        success
        currentVote
      }
    }