mutation UpvoteTeam($teamId: Int!, $userId: Int!, $ipAddress: String!) {
      upvoteTeam(input: { teamId: $teamId, userId: $userId, ipAddress: $ipAddress }) {
        teamId
        success
        currentVote
      }
    }