query GetMetaVotesWithUserVote($categoryId: Int!, $userId: Int!, $ipAddress: String) {
  metaVotesByCategory(
    categoryId: $categoryId
    ipAddress: $ipAddress
    userId: $userId
  ) {
    downvoteCount
    heroId
    upvoteCount
    userId
    userVote
  }
}